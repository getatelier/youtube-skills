# Command Workflows

This directory contains reusable command workflows for agent sessions.

Workflows are shell scripts or markdown files that encode common multi-step operations (e.g., "release a new version", "add a sub-skill").

To use a workflow:

1. Read the relevant workflow file
2. Execute steps in order
3. Update `.agents/learnings.jsonl` if you hit edge cases
